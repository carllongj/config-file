-- 代码注释(根据所处于代码格式自动生成注释符号)模块

return {
  {
    "numToStr/Comment.nvim",
    -- 指定触发加载时机.
    event = {
      "BufReadPost", "BufNewFile",
    },

    config = function()
      -- 加载即可,不需要额外配置.
    end
  },
}