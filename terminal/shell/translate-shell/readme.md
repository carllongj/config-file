### translate-shell 配置文件
* 安装对应的命令
  ```bash
    # ubuntu
    sudo apt install translate-shell

    # arch linux
    sudo pacman -S translate-shell
  ```

* 将配置文件写入到对应的目录(`~/.config/translate-shell/`)下.
```
  {
    :engine  "bing"
    :tl      "zh"
  }
```
 * 其它选项需要在`命令行中`进行指定.
